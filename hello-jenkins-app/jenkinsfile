pipeline {

    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Cloning repository...'
                checkout scm
            }
        }

        stage('Build') {
            steps {

                    sh 'pwd'
            }
        }

        stage('Archive') {
            steps {
                archiveArtifacts artifacts: 'ls -la'
            }
        }
 
    }

    post {

        success {
            echo 'Application built successfully!'
        }

        failure {
            echo 'Build failed.'
        }

        always {
            echo 'Pipeline finished.'
        }

    }

}
